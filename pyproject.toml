[build-system]
build-backend = "poetry.masonry.api"
requires = ["poetry>=0.12"]

[tool]
[tool.poetry]
authors = ["Your Name <you@example.com>"]
description = ""
name = "physicaltemptingloop"
version = "0.1.0"
[tool.poetry.dependencies]
nltk = "^3.5"
python = "^3.8"
[tool.poetry.dev-dependencies]
